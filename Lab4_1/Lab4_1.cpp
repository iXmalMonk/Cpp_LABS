#include<iostream>

int main()
{
	system("chcp 1251");
	int a, x, y, z;
	printf("Размер файла в битах = ");
	scanf_s("%i", &a);
	x = a / 1024 / 8;
	y = (a - (x * 1024 * 8)) / 8;
	z = a - ((x * 1024 * 8) + (y * 8));
	printf("Килобайт = %i\nБайт = %i\nБит = %i", x, y, z);
}

// Дан размер файла в битах. Выразить размер этого файла в формате X килобайтов Y байтов Z бит.